base.url=http://localhost:3000
base.url.admin=http://localhost:3001

admin.username=superadmin
admin.password=123456aA@

customer.username=customer
customer.password=123456aA@

# Browser: chrome | firefox | edge (hoặc msedge)
browser=chrome

implicit.wait.seconds=10
page.load.timeout.seconds=30

# Delay sau mỗi test (giây) để dễ quan sát browser. 0 = tắt, 1-3 = chạy chậm để xem
delay.after.test.seconds=3
